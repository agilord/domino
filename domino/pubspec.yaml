name: domino
description: An incremental DOM library, with support for virtual DOM and components.
version: 0.9.0
repository: https://github.com/agilord/domino/tree/main/domino
topics:
  - dom
  - html
  - virtual-dom
  - javascript
  - incremental-dom

environment:
  sdk: '^3.5.0'

dependencies:
  web: ^1.0.0

dev_dependencies:
  lints: ^4.0.0
  puppeteer: ^3.0.0
  shelf: ^1.2.0
  shelf_static: ^1.1.0
  test: ^1.20.1
  build_runner: ^2.1.7
  build_web_compilers: ^4.0.0
